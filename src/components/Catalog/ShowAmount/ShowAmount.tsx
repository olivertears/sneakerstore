import React, {FC} from 'react';
//@ts-ignore
import cl from './ShowAmount.module.css'

const ShowAmount: FC = () => {
    return (
        <div className={cl.wrap}>
        </div>
    );
};

export default ShowAmount